node_modules
npm-debug.log
.git
.gitignore
README.md
data/*.db
data/*.sqlite
data/sessions.sqlite
# The Dockerfile writes its own from ARG NPM_TOKEN and deletes it after
# `npm install`; the COPY that follows would otherwise put a committed one back.
.npmrc
